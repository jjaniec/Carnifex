(defun ft_init_window ()
  (sdl:with-init ()
    (sdl:window 320 240)
    (sdl:update-display)
    (sdl:with-events ()
      (:quit-event () t)
      (:video-expose-event () (sdl:update-display))
    )
  )
)